import 'package:flutter/material.dart';
import '../../utils/dashboard_constants.dart';

class ProgramDataContent extends StatelessWidget {
  const ProgramDataContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Program Data Analytics - To be implemented',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}