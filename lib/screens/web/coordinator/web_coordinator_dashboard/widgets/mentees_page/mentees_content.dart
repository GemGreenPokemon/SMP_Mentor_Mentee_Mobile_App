import 'package:flutter/material.dart';
import '../../utils/dashboard_constants.dart';

class MenteesContent extends StatelessWidget {
  const MenteesContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Mentees Management - To be implemented',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}